// String, number, boolean, undefined, null, symbol

const nome = 'Leandro'; // String
const num1 = 12; // Number
const num2 = 14.58; // Number
let nomeAluno; // Undefined (automático). Não aponta para algum lugar na memoria.
const sob = null; // Nulo. Não aponta para algum lugar na memoria.
const aprovado = true; // Boolean